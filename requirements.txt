# requirements.txt
#
# Pinned dependencies for reproducibility (ADR-001, IMP-01 M0).
#
# IMPORTANT: The TS2Vec dependency below MUST have its commit hash
# replaced with the actual pinned commit before the first real training
# run (see configs/base.yaml -> ts2vec.pinned_commit and ADR-001).
# A live `pip install git+https://...` without a pinned commit would
# silently change if the upstream maintainer pushes new commits,
# breaking reproducibility (IMP-01 Risk R-01).

# --- Core scientific stack ---------------------------------------------------
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.0
pyyaml==6.0.1

# --- Deep learning ------------------------------------------------------------
torch==2.3.1

# --- TS2Vec (pinned commit — ADR-001) -----------------------------------------
# DEPENDENCY PINNING STATUS: Confirmed (2026-07-03)
#   Repository:   https://github.com/zhihanyue/ts2vec
#   Branch:       main
#   Commit Hash:  b0088e14a99706c05451316dc6db8d3da9351163
#   Status:       Pinned
# See configs/base.yaml ts2vec: block for full provenance notes.
git+https://github.com/zhihanyue/ts2vec.git@b0088e14a99706c05451316dc6db8d3da9351163#egg=ts2vec

# --- Clustering -----------------------------------------------------------
hdbscan==0.8.38.post1

# --- External baselines (M10.5) -----------------------------------------------
hmmlearn==0.3.2

# --- Data acquisition (M1) -----------------------------------------------------
ccxt==4.3.41

# --- Data formats ---------------------------------------------------------------
pyarrow==16.1.0

# --- Statistics (M11, M14) -----------------------------------------------------
statsmodels==0.14.2

# --- Visualization (M12) --------------------------------------------------------
matplotlib==3.9.0
umap-learn==0.5.6

# --- Testing --------------------------------------------------------------------
pytest==8.2.2
pytest-cov==5.0.0
